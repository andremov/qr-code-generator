type LocaleStrings = {
  error: string;
  title: string;
  labels: {
    data: string;
    foreground: string;
    background: string;
  };
  buttons: {
    generate: string;
    download: string;
  };
};

export default LocaleStrings;
